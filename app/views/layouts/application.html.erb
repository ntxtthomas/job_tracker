<!DOCTYPE html>
<html>

<head>
  <title><%= content_for(:title) || "Job Tracker" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
  <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <%# Includes all stylesheet files in app/assets/stylesheets %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <nav style="background-color: #f8f9fa; padding: 1rem; margin-bottom: 2rem; border-bottom: 1px solid #dee2e6;">
    <div style="display: flex; gap: 1rem; align-items: center;">
      <%= link_to "Dashboard", root_path, style: "text-decoration: none; color: #007bff; font-weight: bold;" %>
      <span style="color: #6c757d;">|</span>
      <%= link_to "Companies", companies_path, style: "text-decoration: none; color: #007bff;" %>
      <span style="color: #6c757d;">|</span>
      <%= link_to "Contacts", contacts_path, style: "text-decoration: none; color: #007bff;" %>
      <span style="color: #6c757d;">|</span>
      <%= link_to "Opportunities", opportunities_path, style: "text-decoration: none; color: #007bff;" %>
      <span style="color: #6c757d;">|</span>
      <%= link_to "Interactions", interactions_path, style: "text-decoration: none; color: #007bff;" %>
    </div>
  </nav>

  <div style="padding: 0 1rem;">
    <%= yield %>
  </div>
</body>

</html>
